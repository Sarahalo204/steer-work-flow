import { createFileRoute } from "@tanstack/react-router";import { AppShell } from "@/components/app-shell";import { TaskBoard } from "@/components/task-board";import { PageHeading } from "@/components/steer-ui";import { t,useLocale } from "@/components/steer-context";
export const Route=createFileRoute("/board")({head:()=>({meta:[{title:"Portfolio board — Steer"},{name:"description",content:"All active project work in one execution board."},{property:"og:title",content:"Portfolio board — Steer"},{property:"og:description",content:"All active project work in one execution board."},{property:"og:type",content:"website"},{name:"twitter:card",content:"summary_large_image"}]}),component:Board});function Board(){const {isArabic}=useLocale();return <AppShell><PageHeading eyebrow={t(isArabic,"All projects","كل المشاريع")} title={t(isArabic,"Board","لوحة المهام")} description={t(isArabic,"Every open action across your portfolio.","كل إجراء مفتوح عبر محفظة مشاريعك.")}/><TaskBoard portfolio/></AppShell>}
